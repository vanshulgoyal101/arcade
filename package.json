{
  "name": "tiny-arcade-tests",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Unit tests for the Tiny Arcade games' pure logic.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "build:games": "for g in chromatic digit-span echo flash flashmath hue-hunt interval sprint where word wordle; do (cd $g && npm run build) || exit 1; done && node scripts/clean-urls.mjs",
    "clean:urls": "node scripts/clean-urls.mjs",
    "generate:og": "node scripts/generate-og.mjs"
  },
  "devDependencies": {
    "jsdom": "^25.0.1",
    "sharp": "^0.34.2",
    "vitest": "^2.1.9"
  }
}
